void load_files(string embedding, string corpus, double** word_embedding, map<string, int> word2idx,
        string* idx2word, vector<vector<string>> W, int N) {
    /* param:
     *
     * */
    return;
}
