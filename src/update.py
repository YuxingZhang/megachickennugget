import sys
import nympy as np

def update_z(zvec, d, n, K, varargin):
	# update the vector z_dn of length K from Eq. 7
	# q(z_dn) is a multinomial distribution with q(z_dn=k) = z_dn(k)

	for i in range(K):
	# TODO:

	return zvec

def update_mu(muvec, d,)